THE CUTTING ROOM — COMPLETE HTML/CSS/JS PLATFORM

Open index.html to begin. No login is required. No fake film or evaluation data is included.

For best local browser-storage behavior, serve this folder with a simple static web server, e.g.:
  python3 -m http.server 8080

Then browse to http://localhost:8080/
