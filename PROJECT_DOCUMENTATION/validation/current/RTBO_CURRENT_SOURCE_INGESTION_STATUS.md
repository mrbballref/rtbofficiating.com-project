# RTBO CURRENT SOURCE INGESTION STATUS

**Validation system:** `PROJECT_DOCUMENTATION/validation/current/`  
**Status:** ACTIVE  
**Teaching freeze:** ACTIVE

## 1. Repository source state

At establishment of the current ultimate validation program, the connected GitHub repository does not yet contain the required production platform source trees under `STANDALONE_PLATFORMS/`.

Therefore:

```text
ACTUAL COMPLETE PLATFORM SOURCE IN GITHUB: NO
PROJECT-WIDE SOURCE VALIDATION COMPLETE: NO
PROJECT_WIDE_LOCKED: NO
TEACHING FREEZE: ACTIVE
```

No current source file may receive `VALIDATED` or `LOCKED` merely from requirements, historical snippets, conversation history, screenshots, ZIP descriptions, or previously reported pass states.

## 2. Current evidence that may be used for contract reconstruction

The validation program may use:

- user's latest explicit requirements;
- the current cumulative checkpoint;
- architecture/route/UI/design/filesystem/implementation-plan documents;
- previously approved assets and visual requirements;
- historical source only as reference input;
- user-provided code snippets only as evidence of current local typing state;
- verified external standards and current official documentation.

These can establish the required contract, but **source validation requires actual source**.

## 3. Mandatory ingestion/build order

```text
01_RTBO_Core_Website
02_Got_U_Nex_Ref
03_RefZone_University
04_The_Live_Stream
05_The_Jammed_Up_Bar
06_The_Locker_Room
07_The_Vault
08_The_RefShop
09_The_Cutting_Room
10_The_Lab_Hub
11_Master_CMS_Super_Admin
→ Integrated HTML/CSS/JS Enterprise
→ Freeze Full-Stack Architecture
→ Full-Stack Platform Modules
→ Integrated Full-Stack Enterprise
```

## 4. Source intake/build rules

For each platform:

1. reconcile all current requirements and exclusions;
2. inventory or construct the exact approved filesystem contract;
3. identify real approved assets and ownership;
4. identify historical vs current code;
5. scan every route/link/import/resource reference;
6. detect missing/orphan/duplicate/competing implementations;
7. detect placeholders/dummy code/fake secure behavior;
8. detect secrets/private data before public Git storage;
9. design the complete implementation and downstream dependencies;
10. implement in a validation workspace/branch;
11. run all applicable validation gates;
12. record evidence, commit SHA and content hash;
13. only then mark `VALIDATED` or `LOCKED`.

## 5. Public repository safety

Never commit real secrets or private user/customer/member data, including:

- `.env` files with secrets;
- API keys/tokens;
- private keys/certificates;
- database credentials;
- payment secrets;
- production session secrets;
- real password hashes;
- private messages/notifications;
- private personal, medical or financial records;
- private media not approved for public source control.

Use environment/deployment secret stores for runtime secrets.

## 6. Existing local Core typing state

The user has locally typed part of the restarted Core `index.html`, including early navigation markup. That source is not automatically canonical because the current ultimate rule requires the complete downstream implementation to be validated first.

Known prior review exposed omissions in the earlier teaching process, including document metadata/favicon planning and disclosure hidden-state synchronization. Therefore every existing Core line must be reconciled against the finished validated Core implementation before any teaching resumes.

## 7. No-false-pass rule

Until the complete applicable source and evidence exist:

```text
VALIDATED = prohibited
LOCKED = prohibited
```

The registry remains authoritative.
