# RTBO VALIDATION EXECUTION ORDER — CURRENT

The project is validated in dependency order so later integrations cannot invalidate earlier evidence unnoticed.

## Phase 1 — contract/reference reconciliation for all platforms

Before final source lock, reconcile every platform's latest requirements, approved screenshots/files/assets, external reference products/websites, routes, design system, shared ecosystem requirements and permanent exclusions.

## Phase 2 — standalone HTML/CSS/JS source creation and validation

1. RTBO Core Website
2. Got U Nex Ref
3. RefZone University
4. The Live Stream
5. The Jammed Up Bar!
6. The Locker Room
7. The Vault
8. The RefShop
9. The Cutting Room
10. The Lab Hub
11. Master CMS + Super Admin representational/static interface edition, with no false secure-authority claims

Each is independently built, executed, UI/UX compared, accessibility tested, line-ledgered and locked.

## Phase 3 — integrated HTML/CSS/JS enterprise

Integrate the locked standalone editions, then rerun complete cross-platform route/navigation/account/consent/responsive/accessibility/performance/regression validation. Integration changes that affect a locked source dependency require revalidation.

## Phase 4 — full-stack architecture freeze

Choose and freeze exact runtime/framework/library/database/provider/deployment versions, auth/session architecture, role/permission model, storage, payments, notifications, observability, backups and rollback strategy using current official documentation and security guidance.

## Phase 5 — individual full-stack modules/platforms

Implement and validate each authoritative platform/module, including server-side validation, authorization, persistence, failure states, security, tests and operations.

## Phase 6 — integrated full-stack enterprise

Integrate every locked full-stack module and rerun complete application/API/database/security/payment/upload/notification/performance/accessibility/regression validation.

## Phase 7 — project-wide lock

Generate final immutable release manifest and per-file hashes/ledgers. Only after every gate passes may `PROJECT_WIDE_LOCKED = true` and `TEACHING_FREEZE = false`.

## Phase 8 — teaching

Teach the exact locked source using the RTBO Coursera/Udemy/Zero To Mastery-inspired structured course protocol.

## Phase 9 — public production migration

Only after explicit user approval, deploy the exact locked release under the migration/rollback plan and replace the current public design after production verification.
