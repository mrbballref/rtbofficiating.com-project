# Platform 01 Core validation tools

- `materialize_core_source.py` reconstructs the complete current Core source tree as ordinary repository files.
- The browser/runtime test lives under the adjacent `tests/` directory.
- The materialized source remains a draft until all mandatory validation gates pass.
