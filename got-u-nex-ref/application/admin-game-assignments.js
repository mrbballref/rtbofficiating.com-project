(()=>{'use strict';const KEY='gotUNexRef.adminAssignments.v1';const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function load(){try{return JSON.parse(localStorage.getItem(KEY)||'null')||{assignments:[],drafts:[]}}catch{return{assignments:[],drafts:[]}}}function render(){const s=load(),items=[...(s.assignments||[]),...(s.drafts||[])].sort((a,b)=>String(b.createdAt||'').localeCompare(String(a.createdAt||''))).slice(0,12);$('#assignmentTotal').textContent=`${(s.assignments||[]).length} published · ${(s.drafts||[]).length} drafts`;$('#assignmentEmpty').hidden=items.length>0;$('#assignmentRows').innerHTML=items.map(a=>`<tr><td>${esc(a.date||'—')} ${esc(a.time||'')}</td><td>${esc(a.matchup||a.title||'—')}</td><td>${esc([a.level,a.gender].filter(Boolean).join(' · '))}</td><td>${esc(a.crewSize?`${a.crewSize}-Man`:'—')}</td><td><span class="status-pill ${a.published?'published':''}">${esc(a.published?'Published':'Draft')}</span></td><td>${esc((a.crew||[]).filter(c=>c.role!=='Alternate').map(c=>c.name).filter(Boolean).join(', ')||'—')}</td></tr>`).join('')}render();window.addEventListener('storage',e=>{if(e.key===KEY)render()})})();