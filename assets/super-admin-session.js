// Deprecated: this file used to unconditionally grant Super Admin access to
// anyone who loaded it. It is no longer referenced by any page — each entry
// point now runs a real requireRole('super_admin') check from assets/auth.js
// before rendering or redirecting. Left as an inert stub in case anything
// external still references the path.
